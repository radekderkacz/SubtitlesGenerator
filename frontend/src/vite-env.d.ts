/// <reference types="vite/client" />

// Injected at build time from package.json "version" via Vite `define`
// (configured in both vite.config.ts and vitest.config.ts).
declare const __APP_VERSION__: string
